import { css } from "lit";

export const formatColorStyles = css`
  label {
    align-items: center;
    display: flex;
    gap: 4px;
  }

  input {
    cursor: pointer;
    height: 28px;
    padding: 0;
    width: 32px;
  }
`;
