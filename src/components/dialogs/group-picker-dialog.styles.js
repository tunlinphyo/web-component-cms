import { css } from "lit";

export const groupPickerDialogStyles = css`
  dialog {
    border: 1px solid #999;
    border-radius: 0.5rem;
    padding: 1rem;
  }

  menu {
    display: grid;
    gap: 0.5rem;
    margin: 0;
    padding: 0;
  }
`;
